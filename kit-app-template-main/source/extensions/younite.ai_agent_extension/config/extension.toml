[package]
title = "Younite AI Agent Extension"
description = "AI agent chat via external API with per-avatar sessions"
version = "0.1.0"
authors = ["Younite"]
category = "Feature"

[dependencies]
"younite.messaging_core_extension" = {}
"younite.payload_orchestrator_core_extension" = {}

[[python.module]]
name = "younite.ai_agent_extension"

[build]
include = ["younite/**/*.py", "*.toml"]
