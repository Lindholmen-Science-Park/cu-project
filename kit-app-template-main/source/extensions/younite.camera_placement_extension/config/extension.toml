[package]
title = "Younite Camera Placement Extension"
description = "Place, manage, and rotate cameras at world positions via pick results"
version = "0.1.0"
authors = ["Younite"]
category = "Feature"

[dependencies]
"younite.messaging_core_extension" = {}
"omni.usd" = {}

[[python.module]]
name = "younite.camera_placement_extension"

[build]
include = ["younite/**/*.py", "*.toml"]
