[package]
title = "Younite Incident Extension"
description = "Incident creator: place cubes on nav layer from web UI, rebake NavMesh so paths avoid them"
version = "0.1.0"
authors = ["Younite"]
category = "Feature"

[dependencies]
"younite.messaging_core_extension" = {}
"younite.picking_extension" = {}
"younite.payload_orchestrator_core_extension" = {}
"omni.usd" = {}

[[python.module]]
name = "younite.incident_extension"

[build]
include = ["younite/**/*.py", "*.toml"]
