/** Fade to black duration (ms). */
export const VIEW_TRANSITION_FADE_OUT_MS = 1000;

/** Fade from black duration (ms). */
export const VIEW_TRANSITION_FADE_IN_MS = 1000;

/** Shown centered while the overlay is fully opaque. */
export const DEFAULT_VIEW_TRANSITION_MESSAGE = 'Preparing view\u2026';
